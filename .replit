run = "npm install && npx prisma generate && npx prisma migrate dev --name init && npm run db:seed && npm run dev"
modules = ["nodejs-18"]

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "npm install && npx prisma generate && npx prisma migrate deploy && npm run db:seed && npm start"]
